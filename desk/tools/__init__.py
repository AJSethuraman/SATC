"""Generators for the pages the firm reads, kept importable so they can be tested.

`docket_form` states numbers about the record; `test_docket_form.py` compares the
page it renders against that record, and it can only do that if it can import it.
"""
