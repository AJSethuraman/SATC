[project]
name = "agent-dungeon-arena"
version = "0.1.0"
description = "Deterministic, auditable fantasy dungeon competition for prompt-defined agents"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
agent-arena = "arena.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]

