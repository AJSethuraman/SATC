# Run: the gate on the house brains, agent_sdk — 12 September 2026

Run by the local Claude Code session `ajish-5a` on the forge, on the firm's
explicit permission (the first attempt on 11 September was refused by this
session's permission classifier; see
`runs/20260911T201514Z-agent-sdk.md`). No code was edited.

## Result

**calls: 136, answered by the model: 136 of 136.** Wall clock **358.1 s**
(5 min 58 s), exit code 0. Pack: `code/gate/20260912-034624-agent_sdk`.

## Setup

- Worktree `C:\Users\ajish\SATC-eva`, branch
  `claude/ember-vault-arena-rebuild-dn890n`, **HEAD `d539d35f` at run time**.
  That is before satc-6c's `1c1fcc73` ("read tokens from the SDK's real usage
  field"), so this pack was produced by the adapter *without* that fix. The
  rebase onto `d8b5bb1b` happened after the run.
- `ANTHROPIC_API_KEY` unset; checked again in the shell immediately before the
  command and printed as `ANTHROPIC_API_KEY set: False`. No key printed or
  committed.
- `claude-agent-sdk` 0.2.152, Python 3.12.10, Claude Code 2.1.269.

## Step 4 — the gate

Command, run from `docs/ember-vault-arena/code`:

```
python tools/gate.py --provider agent_sdk --brains brains/house --seeds 3 --rounds 6 --build scout
```

Start 2026-09-12T07:46:24Z, end 2026-09-12T07:52:22Z.

Full output, verbatim:

```
gate pack written to gate\20260912-034624-agent_sdk
# Gate run 20260912-034624-agent_sdk

- provider: **agent_sdk** (model: opus)
- brains: brains/house (8 loaded), all on build **scout**
- seeds: [101, 102, 103], rounds per seed: 6
- calls: 136, answered by the model: 136 of 136
- cost recorded: $8.55 (from the call ledger)

Hand `transcripts/` and `brains/` to two readers who wrote none of the brains. Each fills in `ANSWER_SHEET.md` and saves a JSON answer file. Then:

```
python3 tools/score_gate.py gate/20260912-034624-agent_sdk reader_a.json reader_b.json
```

Do not open `KEY.json` until both sheets are in.
```

(The pack folder is named `20260912-034624`, in the forge's local time; the
run was 07:46–07:52 UTC.)

## The pack

19 files, all committed:

```
ANSWER_SHEET.md    522 bytes
KEY.json           319 bytes   (not opened)
README.md          584 bytes
brains/1.md .. 8.md          1,014–1,194 bytes
transcripts/A.md .. H.md     7,911–13,227 bytes
```

`KEY.json` was not opened. It is committed because the mock pack
`20260911-195102-mock` already on the branch commits its `KEY.json` too — same
shape, same 19 files. **Anyone who is going to be a reader should not open it,
and should take the pack from a checkout rather than reading the diff.**

Checks done on the pack itself, rather than assumed:

- All eight transcripts and all eight brain files are non-empty and the sizes
  are in the same band; no truncated file.
- `ANSWER_SHEET.md` asks for one brain number per character A–H, each number
  used once, saved as JSON.
- The transcripts are valid UTF-8 — curly quotes and em-dashes read correctly
  through a UTF-8 reader. (They look like mojibake in a Windows console that
  defaults to the ANSI code page; that is the console, not the file.)

## Observations for satc-6c

1. **136 calls, not 144.** Three seeds × six rounds × eight contestants would
   be 144. Eight calls did not happen. Not diagnosed here — worth knowing
   whether that is the engine eliminating or skipping a contestant by design.
2. **`cost recorded: $8.55`** is the SDK's reported figure over 136 calls
   (about $0.063 each, consistent with the smoke test's $0.072). With no API
   key set this should be on the subscription; this session cannot see the
   account's billing to confirm what was charged.
3. Nothing failed: no network fallback, no panic, no invalid line — the
   README's own count says 136 of 136 answered.

## Next per the checklist

Step 3 of the morning checklist is a human step: hand `transcripts/` and
`brains/` to two readers who wrote none of the brains, collect
`reader_a.json` and `reader_b.json`, then run

```
python tools/score_gate.py gate/20260912-034624-agent_sdk reader_a.json reader_b.json
```

PASS is both readers at six of eight or better. Nothing in M3 starts before
that has been read.
