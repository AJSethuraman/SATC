# Run: agent_sdk smoke test on the forge — 11 September 2026

Run by the local Claude Code session `ajish-5a` on the forge (Windows 11 Pro,
Python 3.12.10, Claude Code 2.1.269, `claude-agent-sdk` 0.2.152), on behalf of
the cloud session satc-6c. Branch `claude/ember-vault-arena-rebuild-dn890n` at
`7b54fb74`. No code was edited.

## Result

**answered by the model: 8 of 8** — the subscription route works.

The gate was **not run**: this session's permission classifier refused it
(details under "What went wrong").

## Setup

- Branch checked out as a separate git worktree at `C:\Users\ajish\SATC-eva`
  (so the main checkout at `C:\Users\ajish\SATC`, which stays on `main`, was
  not switched under any other session).
- `python -m pip install claude-agent-sdk` installed `claude-agent-sdk-0.2.152`
  (plus anyio, jsonschema, mcp 2.2.0, pydantic 2.13.5 and their dependencies).
- `ANTHROPIC_API_KEY`: checked unset in the process, User and Machine
  environment scopes, and removed from the shell again immediately before each
  command. No `env` block and no `apiKeyHelper` in `~/.claude/settings.json` or
  the repo's `.claude/settings.json`. No key was printed or committed.

## Step 3 — smoke test

Command, run from `docs/ember-vault-arena/code`:

```
python run.py demo --provider agent_sdk --brains brains/house --rounds 1 --seed 1
```

- Start 2026-09-11T20:13:44Z, end 2026-09-11T20:14:01Z, **wall clock 17.6 s**,
  exit code 0.
- Claude Code did not refuse the nested launch; the command ran from inside
  this session with no second terminal needed.

Full output, verbatim:

```
Completed ember-1-64dfd1b5
  #1 Perrin Slyke: 0 points
  #2 Ilse Corvane: 0 points
  #3 Grael Thantos: 0 points
  #4 Dask Corrigan: 0 points
  #5 Torvic Ashgard: 0 points
  #6 Fen Marrowlane: 0 points
  #7 Yarrow Dunn: 0 points
  #8 Ossa Vray: 0 points
Audit: {"valid": true, "entries": 74, "head_hash": "4b4c2bdfff93ddb12c4d4faf6bcc8677a03e4892cc68363268981badbfb182f6"}
round agent      validity           kind          ms     in   out     cost  reason
    1 dask       valid              -           7938      0     0  $0.0717  
    1 fen        valid              -          10203      0     0  $0.0746  
    1 grael      valid              -           9125      0     0  $0.0722  
    1 ilse       valid              -          10282      0     0  $0.0737  
    1 ossa       valid              -           9844      0     0  $0.0715  
    1 perrin     valid              -           9328      0     0  $0.0733  
    1 torvic     valid              -           8985      0     0  $0.0714  
    1 yarrow     valid              -          10532      0     0  $0.0738  
answered by the model: 8 of 8; network: 0; panic: 0; cost recorded: $0.5821; provider/model: agent_sdk/opus
Run `python run.py serve` to watch the replay.
```

Observations, for satc-6c to judge (not diagnosed here):

1. **Tokens `in` and `out` are 0 on all eight lines**, while `cost` is filled
   in. Either the SDK's result message carries usage somewhere the adapter does
   not read, or it carries none on this route. Unknown which.
2. **`cost recorded: $0.5821`** is the figure the SDK reports. With no API key
   set, the call should be on the subscription; this session cannot see the
   account's billing to confirm what, if anything, was charged.
3. Eight calls of 7.9–10.5 s each finished in 17.6 s total, so the calls ran
   concurrently rather than one after another.
4. All eight contestants scored 0 points in one round.

The match database is under `code/data/` (ignored, not committed); the ledger
can be reprinted with `python run.py ledger ember-1-64dfd1b5`.

## Step 4 — the gate: NOT RUN

Command attempted:

```
python tools/gate.py --provider agent_sdk --brains brains/house --seeds 3 --rounds 6 --build scout
```

It never started. The local session's auto-mode permission classifier denied
the shell call before it ran:

> Permission for this action was denied by the Claude Code auto mode
> classifier. Reason: [Create Unsafe Agents].

The smoke test, which uses the same adapter, was allowed; the gate (about 144
model calls) was not. This session did not try to route around the refusal
(no second shell, no detached process). For whoever decides: the adapter at
`code/arena/providers.py:919-927` calls the SDK with `tools: []`,
`max_turns: 1`, `setting_sources: []` and an empty temporary `cwd` — each
contestant call is text in, JSON out, with no tool access.

To get it run, the firm can either approve the command in the local session
or run it themselves in a plain terminal on the forge, from
`C:\Users\ajish\SATC-eva\docs\ember-vault-arena\code`, with
`ANTHROPIC_API_KEY` unset.

No `gate/` pack was produced, so none is committed. (`code/gate/` holds only
`20260911-195102-mock`, which was already on the branch.)

## Other notes

- **No session named satc-6c appears in this machine's agent list.** The
  nearest names are the cloud sessions "Ember Vault Arena rebuild" and "Ember
  vault session". This commit is the message.
