# The two plugin commands, run on the forge — 12 September 2026

FORGE.md §09:55Z, run verbatim and in order at the firm's instruction. Both
exited 0. Neither asked for a permission, and nothing was granted. They touched
the Claude Code plugin install only.

Run from `docs/ember-vault-arena/code` (the orders say any folder).

## 1. `claude plugin marketplace update satc`

```
Updating marketplace: satc...Refreshing marketplace cache (timeout: 120s)…
✔ Successfully updated marketplace: satc
```

exit 0.

## 2. `claude plugin update canon`

```
Checking for updates for plugin "canon" at user scope…
✔ Plugin "canon" updated from 1.16.0 to 1.17.0 for scope user. Restart to apply changes.
```

exit 0.

## The version, confirmed three ways

**1.17.0, as expected.**

- The update line itself: `updated from 1.16.0 to 1.17.0 for scope user`.
- `claude plugin list`:
  ```
    ❯ canon@satc
      Version: 1.17.0
      Scope: user
      Status: ✔ enabled
  ```
- On disk, `C:\Users\ajish\.claude\plugins\cache\satc\canon\` now holds a
  `1.17.0` directory alongside the older versions.

## One caveat the output states and I did not act on

`Restart to apply changes.` **The update is installed but not loaded.** This
session — and any other Claude Code session already open on this machine — is
still running the plugin code it loaded at start, which for this one was
1.16.0. The rulings in 1.17.0 reach a session when it next starts. I did not
restart anything: restarting sessions is the firm's call, not a step in these
orders.

The other two installed plugins were untouched and unchanged: `desk@satc`
0.21.0 and `occam@occam` 0.4.0, both enabled.

## The rest of the 09:55Z section

**The two-round smoke has already run.** It was pushed before these orders were
read, as `runs/20260912T093832Z-agent-sdk-smoke-2rounds.md` (commit
`0ca19369`). Answer, in one line: round 2 does **not** read round 1's write —
`cached` is exactly 2,060 on all sixteen calls and `wrote` rises rather than
collapsing.

**The Haiku line: I do not have it.** Nothing in `probe_sdk.py`'s output, the
ledger, or anything visible to this session names what the
`claude-haiku-4-5-20251001` call is doing — only its shape (~3,400 tokens read,
13–17 written, on every arena call, ~$0.0035). Per the same orders I did not
chase it further. If it matters, it is visible from inside the SDK's own debug
output, which is the cloud's to enable.

## Standing down

Nothing is running on the forge: no server, no background job, no watcher. The
worktree is clean and every result this session produced is under `runs/`.
