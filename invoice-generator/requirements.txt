Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-WTF==1.2.1
Flask-Limiter==3.8.0
sentry-sdk[flask]==2.13.0
SQLAlchemy==2.0.34
Jinja2==3.1.4
# Used directly by app.py to validate raster logo uploads. It was present
# only transitively via the PDF engines below; dropping one of those made
# every raster logo upload fail as though the file were corrupt.
#
# A FLOOR RATHER THAN A PIN, deliberately, against this file's own convention.
# The point of the line is that the dependency is DECLARED; pinning it to an
# exact version nobody here has run would trade one untested assumption for
# another. The PDF engines resolve the exact version, as they always did.
Pillow>=10.4
WeasyPrint==62.3
pydyf==0.10.0
xhtml2pdf==0.2.16
stripe==10.5.0
python-dotenv==1.0.1
gunicorn==22.0.0
