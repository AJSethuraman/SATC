fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
python-multipart>=0.0.9
pillow>=10.0
numpy>=1.26

# Test-only. Listed because the suite exercises the HTTP layer through
# fastapi.TestClient, which needs httpx — without it those tests error rather
# than skip, and a test that only passes where somebody happened to pip
# install something is not a passing test.
pytest>=8.0
httpx>=0.27
